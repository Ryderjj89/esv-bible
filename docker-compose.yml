version: '3.8'

services:
  esv-bible:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./bible-data:/app/bible-data:ro
    environment:
      - NODE_ENV=production
    restart: unless-stopped
